---
- hosts: k3s_nodes
  tasks:
    - name: paquetes instalados
      become: true
      apt:
        pkg:
          # Para longhorn
          - open-iscsi

- hosts: k3s_nodes
  roles:
    - k3s-ansible/roles/prereq
    - k3s-ansible/roles/download

- hosts: k3s_pi
  roles:
    - k3s-ansible/roles/raspberrypi

- hosts: k3s_nodes
  roles:
    - k3s-ansible/roles/k3s/node
