---
- hosts: k3s_cluster
  roles:
    - k3s-ansible/roles/prereq
    - k3s-ansible/roles/download

- hosts: k3s_pi
  roles:
    - k3s-ansible/roles/raspberrypi

- hosts: k3s_master
  become: true
  tasks:
    - apt:
        name: open-iscsi
        state: present

- hosts: k3s_master
  roles:
    - k3s-ansible/roles/k3s/master
    - k3s-access

# - hosts: k3s_nodes
#   roles:
#     - k3s-ansible/roles/k3s/node
